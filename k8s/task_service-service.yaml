apiVersion: v1
kind: Service
metadata:
  name: task-service
  labels:
    app: task-service
    spring-boot: "true"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      protocol: TCP
  selector:
    app: task-service