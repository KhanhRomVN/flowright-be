apiVersion: v1
kind: Service
metadata:
  name: workspace-service
  labels:
    app: workspace-service
    spring-boot: "true"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      protocol: TCP
  selector:
    app: workspace-service